# HAProxy example with .NET Web Api

## Commands

- build image: `docker build -t api-gateway .`
- run: `docker compose up`

## Check

Go to <http://localhost:8080/>, <http://localhost:8080/swagger/>
